# Assignment 2 – Set C – Q1: Instructor-Provided Values

## Lab-book task
The workbook leaves the Set C table names, columns, constraints, and relationship blank for the instructor to fill in. Provide a fill-in template rather than inventing the missing source values.

## Solution
The source workbook explicitly says: **“Instructor should fill in the blanks with appropriate values.”** Therefore the exact Set C schema is not specified in the supplied source. Use this template once the instructor supplies the blanks:

`CREATE TABLE <TABLE1>(<columns and constraints>);`

`CREATE TABLE <TABLE2>(<columns and constraints>);`

`-- Relationship: <one-to-one / one-to-many / many-to-many>`

For a many-to-many relationship, add a junction table containing both foreign keys and a composite primary key.
